{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for app windows. All privileged work (sidecar, notifications, autostart, health polling) happens in Rust — the webviews get core defaults only.",
  "windows": ["main", "dashboard"],
  "permissions": ["core:default"]
}
