# Secrets & WhatsApp session — NEVER commit these
.env
auth/
auth.bak*/
auth-corrupt*/
logs/
qr.png
*.log

# WhatsApp media staging (only relevant if WORKDIR is this repo)
inbox/
outbox/

# Dependencies & build
node_modules/
dist/
*.bun-build

# OS
.DS_Store

# Persistent message store (private conversation data)
data/

# Tauri tray app (app/) build outputs
app/node_modules/
app/src-tauri/target/
app/src-tauri/binaries/
app/src-tauri/gen/
